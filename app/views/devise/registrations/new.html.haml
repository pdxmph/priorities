%h3.col-sm-offset-2
  Sign Up

%br
= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:class => "form-horizontal"} ) do |f|
  = devise_error_messages!
  .form-group
    = f.label :full_Name, :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.text_field :fullname, autofocus: true, :class => 'form-control'
  .form-group
    = f.label :email, :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.email_field :email, :class => 'form-control'
  .form-group
    = f.label "Manager", :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.check_box :admin, :class => 'form-control'
      .help-block
        %i
          Check this box if you're a manager. In this app, managers can create and manage teams but 
          are otherwise harmless.
        
  .form-group
    = f.label "Team", :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.select(:team_id, [["None", nil]] + Team.all.collect {|t| [t.name, t.id]}, {}, {:class => 'form-control'})
      .help-block
        %i
          If you're a manager and your team doesn't exist yet, go ahead and sign up then create your team 
          from the "Team" menu. Once you've created it, you'll be added to it automatically.
          
  .form-group
    = f.label :password, :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.password_field :password, autocomplete: "off", :class => 'form-control'
      - if @validatable
        %em
          (#{@minimum_password_length} characters minimum)
  .form-group
    = f.label :confirm, :class => 'col-sm-2 control-label'
    .col-sm-4
      = f.password_field :password_confirmation, autocomplete: "off", :class => 'form-control'
  .actions.col-sm-offset-2
    .col-sm-4
      = f.submit "Sign Up", :class => 'btn btn-primary'


     
